[build]
  command = "vite build"
  publish = "dist/public"
  functions = "netlify/functions"

# Production context
[context.production]
  command = "vite build"

# Deploy Preview context
[context.deploy-preview]
  command = "vite build"

# Branch Deploy context
[context.branch-deploy]
  command = "vite build"

# Redirect all routes to index.html for SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Proxy API requests to Netlify serverless functions
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200

# Dev server settings
[dev]
  command = "npm run dev"
  port = 8888
  targetPort = 5000
  publish = "dist/public"
  autoLaunch = true

# Serverless function bundler
[functions]
  node_bundler = "esbuild"

# Custom build plugin
[[plugins]]
  package = "/netlify/build-plugins/vite-build.js"
